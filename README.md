# DSP — Credit Card Fraud Detection

ML-powered fraud detection pipeline with FastAPI, Airflow, Great Expectations, and Grafana.

## Setup
1. Copy `.env.example` to `.env` and fill in values
2. `docker compose up --build`
